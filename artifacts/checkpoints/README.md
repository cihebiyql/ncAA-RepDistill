# Checkpoint Placeholder

Expected local files (not committed by default):

- `artifacts/checkpoints/phase1_mainline_epoch30.pt`
- `artifacts/checkpoints/phase2_best_downstream_best.pt` (optional)

Use:

```bash
bash scripts/link_local_large_assets.sh
```
