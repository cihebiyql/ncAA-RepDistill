# Phase1 Teacher Cache Placeholder

Place or link the following caches here before running `phase1_mainline`:

- `data/teacher_cache/phase1/esm2/{train_features.pt,val_features.pt}`
- `data/teacher_cache/phase1/chemberta/{train_features.pt,val_features.pt}`

Use:

```bash
bash scripts/link_local_large_assets.sh
```
